<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="upogarden.list">
      <field name="name">UPOGARDEN list</field>
      <field name="model">upogarden.upogarden</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="upogarden.action_window">
      <field name="name">UPOGARDEN window</field>
      <field name="res_model">upogarden.upogarden</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="upogarden.action_server">
      <field name="name">UPOGARDEN server</field>
      <field name="model_id" ref="model_upogarden_upogarden"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="UPOGARDEN" id="upogarden.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="upogarden.menu_1" parent="upogarden.menu_root"/>
    <menuitem name="Menu 2" id="upogarden.menu_2" parent="upogarden.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="upogarden.menu_1_list" parent="upogarden.menu_1"
              action="upogarden.action_window"/>
    <menuitem name="Server to list" id="upogarden" parent="upogarden.menu_2"
              action="upogarden.action_server"/>
    -->
  </data>
</odoo>